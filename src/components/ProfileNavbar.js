import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const profileNavbar = () => {
  return (
    <SafeAreaView styles={styles.navbar}>

    </SafeAreaView>
  )
}

export default profileNavbar

const styles = StyleSheet.create({
  navbar: {
    position: absolute,
    bottom: 0,
  }, j
})